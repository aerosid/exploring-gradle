subprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()
    }
    dependencies {
        testImplementation group: 'junit', name: 'junit', version: '4.13.2'
    }    
}
task javaVersion(type: Exec) {
  executable "java"
  args "--version"
}